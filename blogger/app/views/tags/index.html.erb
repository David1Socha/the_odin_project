<h1>Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag)%></li>
    <br>
  <% end %>
</ul>
<hr>
<ul id="menu">
  <li><%= link_to "Back to Articles", articles_path %></li>
</ul>